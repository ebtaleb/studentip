Web application made using node.js and mongodb.
Can be deployed locally, on a server and IBM Bluemix as well.

Prerequisites : Run mongodb as a daemon.

* Type node bin/www.
* Open http://localhost:3000


* BlueMix deployment
